# Заголовок первого уровня (Markdown)

Это обычный параграф с **жирным текстом** и *курсивом*.

## Таблица для проверки (Markdown)

| Имя    | Возраст | Город      |
| :----- | :-----: | :----------: |
| Анна   |   25    |  Москва     |
| Иван   |   30    |  Санкт-Петербург |
| Ольга  |   28    |  Казань     |

### Код с подсветкой синтаксиса

```js
// Проверка подсветки кода
function greet(name) {
  return `Привет, ${name}!`;
}
console.log(greet('Мир'));

---

Проверка JSX-компонентов

Вот как выглядит использование React-компонента прямо в Markdown:

{/* Это комментарий внутри MDX. Он не должен отображаться. */}

<button
style={{
backgroundColor: '#646cff',
color: 'white',
border: 'none',
padding: '10px 20px',
borderRadius: '8px',
cursor: 'pointer',
fontSize: '16px',
marginBottom: '20px'
}}
onClick={() => alert('MDX работает!')}



Нажми на меня (интерактивная кнопка)
</button>

<div style={{ padding: '20px', border: '2px dashed #3b82f6', borderRadius: '10px', marginTop: '20px' }}>
  <p style={{ margin: 0, color: '#3b82f6', fontWeight: 'bold' }}>
    ✅ Если ты видишь эту синюю рамку и кнопка выше работает, значит MDX парсится корректно!
  </p>
</div>Импорт (необязательно для проверки)

Если твой редактор поддерживает импорты (как в Next.js или Docusaurus), эта строка не должна вызывать ошибку, если файл TestComponent не найден, но в идеале редактор просто выделит ее как код.

import { TestComponent } from './components'; // Эта строка может подчеркиваться, если нет такого компонента, но синтаксис должен быть подсвечен
